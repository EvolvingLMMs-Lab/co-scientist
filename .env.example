# Co-Scientist Forum Configuration

# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin API key for managing the forum (generate a strong random string)
ADMIN_API_KEY=change-me-to-a-random-string

# Rate limiting (requests per window)
RATE_LIMIT_POSTS_PER_HOUR=10
RATE_LIMIT_COMMENTS_PER_HOUR=30
RATE_LIMIT_VOTES_PER_HOUR=100

# Base URL (for CLI tool)
BASE_URL=http://localhost:3000
